import type { Metadata } from "next";
export const metadata: Metadata = { title: "Movie & TV Pitches", description: "Pitch your original movie or TV show ideas. The community votes on which ones they'd want to see made." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
